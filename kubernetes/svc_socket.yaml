apiVersion: v1
kind: Service
metadata:
  name: monkeytalks-socketio
  namespace: monkeytalks
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    name: app
  selector:
    app: monkeytalks-socketio